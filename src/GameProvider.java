import java.io.FileNotFoundException;
import java.util.Random;

public class GameProvider {

    static void gameProvider() throws FileNotFoundException {

        FileReader fileReader = new FileReader();
        fileReader.readFile();

    }
}
